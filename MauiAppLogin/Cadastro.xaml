<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppLogin.Cadastro"
             Title="Cadastro">
    <ContentPage.Background>
        <LinearGradientBrush>
            <GradientStop Color="#00ceff" Offset="0.1"/>
            <GradientStop Color="#995798" Offset="0.8"/>
        </LinearGradientBrush>
    </ContentPage.Background>

    <VerticalStackLayout VerticalOptions="Center"
                         Margin="20"
                         Spacing="10">
        <Label 
            Text="Cadastro"
            HorizontalOptions="Center"
            FontSize="Large"
            Margin="30"/>

        <Label 
            Text="Usuário:"
            HorizontalTextAlignment="Start"/>
        <Entry x:Name="txt_usuario_cadastro"/>

        <Label
            Text="Senha:"
            HorizontalOptions="Start"/>
        <Entry x:Name="txt_senha_cadastro" IsPassword="True"/>

        <Label
            Text="Confirmação de senha:"
            HorizontalOptions="Start"/>
        <Entry x:Name="txt_confirma_senha_cadastro" IsPassword="True"/>

        <Button 
            x:Name="btn_cadastro_cadastro"
            Text="Cadastrar-se"
            Clicked="btn_cadastro_cadastro_Clicked"/>
    </VerticalStackLayout>
</ContentPage>